<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.obdreader2">

    <!-- Bluetooth Classic (SPP) – Android 12 előtt -->
    <uses-permission android:name="android.permission.BLUETOOTH"
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN"
        android:maxSdkVersion="30" />

    <!-- Bluetooth Classic + BLE – Android 12+ -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />

    <!-- Helymeghatározás – régebbi Android BT scan-hez szükséges -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- BLE feature – required=false, mert Classic BT-t is támogatunk -->
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="false" />

    <!-- Értesítések (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- CPU ébren tartása képernyőzár alatt a Bluetooth polling zavartalanságáért -->
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <!-- Előtér szolgáltatás az aktív OBD kapcsolathoz (folyamat életben tartása képernyőzár alatt) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE"/>
    <application
        android:label="ÓE-BD2"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Bluetooth eszköz csatlakozás figyelő (auto-connect kocsi indításkor).
             A permission attribute biztosítja, hogy csak BLUETOOTH_CONNECT engedéllyel
             rendelkező rendszerkomponens (azaz maga az Android BT-stack) kézbesíthet
             ide intent-et — külső app nem tud hamis ACL_CONNECTED-et küldeni. -->
        <receiver
            android:name=".BluetoothConnectReceiver"
            android:exported="true"
            android:permission="android.permission.BLUETOOTH_CONNECT">
            <intent-filter>
                <action android:name="android.bluetooth.device.action.ACL_CONNECTED"/>
            </intent-filter>
        </receiver>

        <!-- Előtér szolgáltatás: OBD polling folytatása képernyőzár alatt -->
        <service
            android:name=".OBDForegroundService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />
    </application>
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>